# =============================================================================
# AUTO-GENERATED by ai-guardrails-generate
# Source: .guardrails-exceptions.toml
# Do not edit this file directly. Edit .guardrails-exceptions.toml and re-run:
#   ai-guardrails-generate
# =============================================================================

target-version = "py311"
line-length = 88
indent-width = 4
fix = false
unsafe-fixes = false

[lint]
select = ["ALL"]
ignore = [
  "COM812",
  "COM819",
  "D206",
  "D300",
  "E111",
  "E114",
  "E117",
  "ISC001",
  "ISC002",
  "Q000",
  "Q001",
  "Q002",
  "Q003",
  "W191",
  # Project wraps the `gh` CLI — subprocess calls to it are intentional
  "S603", # subprocess call with variable input (all calls go through run_gh)
  "S607", # partial executable path (always "gh")
  # HTTP client uses hardcoded HTTPS URL constant (OpenRouter API)
  "S310", # urllib audit — URL not user-controlled
]

[lint.pydocstyle]
convention = "google"

[lint.flake8-annotations]
allow-star-arg-any = false
ignore-fully-untyped = false
mypy-init-return = true
suppress-dummy-args = false
suppress-none-returning = false

[lint.flake8-type-checking]
strict = true
runtime-evaluated-base-classes = ["pydantic.BaseModel"]

[lint.isort]
force-single-line = false
force-sort-within-sections = true
known-first-party = []
combine-as-imports = true
split-on-trailing-comma = true
force-to-top = ["__future__"]
required-imports = ["from __future__ import annotations"]

[lint.pep8-naming]
classmethod-decorators = [
  "classmethod",
  "pydantic.validator",
  "pydantic.field_validator",
]
staticmethod-decorators = ["staticmethod"]

[lint.mccabe]
max-complexity = 10

[lint.pylint]
max-args = 5
max-bool-expr = 3
max-branches = 10
max-locals = 10
max-nested-blocks = 3
max-positional-args = 5
max-public-methods = 15
max-returns = 6
max-statements = 40

[lint.flake8-bugbear]
extend-immutable-calls = ["fastapi.Depends", "fastapi.Query", "fastapi.Path"]

[lint.flake8-comprehensions]
allow-dict-calls-with-keyword-arguments = false

[lint.flake8-quotes]
inline-quotes = "double"
multiline-quotes = "double"
docstring-quotes = "double"

[lint.flake8-tidy-imports]
ban-relative-imports = "all"

[lint.flake8-tidy-imports.banned-api."typing.Optional"]
msg = "Use `X | None` instead"

[lint.flake8-tidy-imports.banned-api."typing.Union"]
msg = "Use `X | Y` instead"

[lint.flake8-tidy-imports.banned-api."typing.List"]
msg = "Use `list[X]` instead"

[lint.flake8-tidy-imports.banned-api."typing.Dict"]
msg = "Use `dict[X, Y]` instead"

[lint.flake8-tidy-imports.banned-api."typing.Set"]
msg = "Use `set[X]` instead"

[lint.flake8-tidy-imports.banned-api."typing.Tuple"]
msg = "Use `tuple[X, ...]` instead"

[lint.flake8-errmsg]
max-string-length = 50

[lint.flake8-pytest-style]
fixture-parentheses = true
mark-parentheses = true
parametrize-names-type = "csv"
parametrize-values-type = "list"
raises-require-match-for = [
  "ValueError",
  "TypeError",
  "KeyError",
  "RuntimeError",
]

[lint.per-file-ignores]
"tests/**/*.py" = [
  "S101",
  "ARG001",
  "ARG002",
  "ARG005",
  "PLR2004",
  "D103",
  # Annotations are noise in test files — tests document behavior, not types
  "ANN001",
  "ANN002",
  "ANN003",
  "ANN201",
  "ANN202",
  "ANN401",
]
"__init__.py" = ["D104", "F401"]
# CLI tool uses print() for user-facing output
"src/guardrails_review/cli.py" = ["T201"]
"src/guardrails_review/reviewer.py" = ["T201"]
"conftest.py" = ["D100", "D103"]

[format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "lf"
docstring-code-format = true
docstring-code-line-length = 80
